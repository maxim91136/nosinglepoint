// Muted bio-violet/turquoise/amber, not neon. Shared between the scene and the legend.
export const COLOR_BITCOIN = "#c99a4a";
export const COLOR_TOR = "#3fc9c2";
// Cross-layer connection: an ASN that hosts both Bitcoin nodes and Tor
// relays, i.e. a single point of failure shared by two "independent"
// networks. Lives on the link between clusters, not on a node color.
export const COLOR_LINK = "#9376d1";
