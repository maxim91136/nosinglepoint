// Bioluminescent forest palette: warm ember (Bitcoin), electric cyan-blue
// (Tor), magenta-violet bloom (cross-layer). Shared between the scene, the
// legend, and the score table.
export const COLOR_BITCOIN = "#e8a35c";
export const COLOR_TOR = "#4dd8f0";
// Cross-layer connection: an ASN that hosts both Bitcoin nodes and Tor
// relays, i.e. a single point of failure shared by two "independent"
// networks. Lives on the root-link between clusters, not on a node color.
export const COLOR_LINK = "#c968d6";
